
Example of the synopsis plugin
==============================
 
## Frequency / En / 3 sentences

> LS(1) User Commands LS(1) ls - list directory contents List information about the FILEs (the current directory by default). Sort entries alphabetically if none of -cftuvSUX nor --sort is Mandatory arguments to long options are mandatory for short options

## Luhn / En / 10 sentences

> LS(1) User Commands LS(1) ls - list directory contents List information about the FILEs (the current directory by default). Sort entries alphabetically if none of -cftuvSUX nor --sort is Mandatory arguments to long options are mandatory for short options
