FEATURE: `<TITLE>`
========================================
`<Optional Draft>`
`<Optional Planned release>`
Created: `<date>`
Closed: `<date>`
Created by: `<Author>`

## Description

instead of writing a full directive, write `mb.type(param=value)` for inline text. Eg

version = `mb.query_path(source='pyproject.toml', path=version)`

Note see how this relates / can be integrated/merged with `doc\project\backlog\open\features\v2.0\feat_template_plugin.md`.

`<optional link to original / related feature(s)>`

`<optional dependencies on other feature(s)>`

## Acceptance criteria

`<List of testable outcomes or DISCUSS if more discussion is warranted>`

## Comments

`<Optional comments in the form DD/MM/YY Author: comment>`