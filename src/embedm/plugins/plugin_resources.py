from types import SimpleNamespace

str_resources = SimpleNamespace(
    registry_show_len_plugins="discovered {len_plugins} plugins.",
    err_plugin_load_failed="failed to load plugin '{name}': {exc}",
)
